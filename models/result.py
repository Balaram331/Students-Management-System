class Result:

    def __init__(
        self,
        student_id,
        total_marks,
        percentage,
        grade
    ):

        self.student_id = student_id

        self.total_marks = total_marks

        self.percentage = percentage

        self.grade = grade